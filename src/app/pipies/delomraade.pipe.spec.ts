import { DelomraadePipe } from './delomraade.pipe';

describe('DelomraadePipe', () => {
  it('create an instance', () => {
    const pipe = new DelomraadePipe();
    expect(pipe).toBeTruthy();
  });
});
